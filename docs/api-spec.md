# API Specification

Reference table lives in `PRD.md` section 8. Once endpoints are
implemented, replace this file with the generated OpenAPI schema
(FastAPI serves it automatically at `/docs` and `/openapi.json`).
