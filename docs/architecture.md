# Architecture Notes

See `PRD.md` section 3 for the authoritative diagrams (block diagram,
sequence diagram, ER diagram, Gantt roadmap — all in Mermaid, rendered
automatically on GitHub and in most Markdown viewers).

This file is for deeper implementation notes that don't belong in the
PRD itself — add design decisions, trade-offs, and "why we didn't do X"
notes here as the project evolves.
