# Diagrams

All architecture diagrams are maintained as Mermaid code directly
inside `PRD.md` (block diagram, sequence diagram, ER diagram, Gantt
chart) so they never drift out of sync with the written spec.

If you export a diagram as a static image (e.g. for a slide deck),
save it here and reference it from the relevant doc.
