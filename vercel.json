{
  "buildCommand": "[ -d frontend ] && cd frontend && npm run build || npm run build",
  "installCommand": "[ -d frontend ] && cd frontend && npm install || npm install",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_API_BASE_URL": "https://semantic-graph-chat.onrender.com",
    "NEXT_PUBLIC_WS_URL": "wss://semantic-graph-chat.onrender.com/ws/chat"
  }
}
